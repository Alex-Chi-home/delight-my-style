# 🚀 Полное руководство по работе с миграциями Supabase

## 📚 Оглавление
1. [Что такое миграции и как они работают](#что-такое-миграции)
2. [Что мы сделали - пошаговый разбор](#что-мы-сделали)
3. [Команды которые использовались](#команды-которые-использовались)
4. [Работа с seed данными](#работа-с-seed-данными)
5. [Best Practices для Production](#best-practices-для-production)
6. [Деплой через Pull Request (Production способ)](#деплой-через-pull-request)
7. [Альтернативные способы деплоя](#альтернативные-способы-деплоя)
8. [Откат миграций](#откат-миграций)
9. [Troubleshooting](#troubleshooting)

---

## 🎯 Что такое миграции

**Миграции** - это версионированные SQL-скрипты, которые изменяют структуру базы данных.

### Как работают миграции:

```
┌─────────────────────────────────────────────────────────┐
│  Миграции = История изменений БД (как Git для кода)    │
└─────────────────────────────────────────────────────────┘

Файл миграции:
  20260210085414_create_brands_table.sql
  └─┬──┘└─┬─┘└─┬┘ └──────────┬──────────┘
    │     │   │              └─ Описание изменения
    │     │   └─ Секунды
    │     └─ Время (HH:MM:SS)
    └─ Дата (YYYY-MM-DD)

Порядок применения:
  1. Supabase читает папку supabase/migrations/
  2. Сортирует файлы по timestamp (дате в имени)
  3. Применяет миграции по порядку
  4. Запоминает какие миграции уже применены (таблица schema_migrations)
  5. При следующем запуске применяет только новые миграции
```

### Таблица отслеживания миграций:

```sql
-- Supabase автоматически создает эту таблицу
SELECT * FROM supabase_migrations.schema_migrations;

-- Результат:
version        | statements | name
---------------|------------|---------------------------
20260127095621 | {...}      | Initial schema
20260210085414 | {...}      | create_brands_table
20260210085846 | {...}      | add_brand_to_products
```

---

## 📝 Что мы сделали

### Шаг 1: Создали таблицу `brands`

**Цель:** Добавить таблицу для хранения информации о брендах одежды

**Команда:**
```bash
supabase migration new create_brands_table
```

**Что создалось:**
- Файл: `supabase/migrations/20260210085414_create_brands_table.sql`

**Что внутри миграции:**
1. ✅ Создание таблицы `brands` с полями:
   - `id` (UUID, primary key)
   - `name` (TEXT, уникальное)
   - `description`, `logo_url`, `website`, `country`
   - `founded_year` (с проверкой 1800-2026)
   - `is_active` (для мягкого удаления)
   - `created_at`, `updated_at` (автоматические)

2. ✅ Индексы для производительности:
   - `idx_brands_name` - быстрый поиск по имени
   - `idx_brands_is_active` - фильтрация активных

3. ✅ Триггер для автообновления `updated_at`

4. ✅ Row Level Security (RLS) политики:
   - Все могут читать активные бренды
   - Только админы могут создавать/изменять/удалять

5. ✅ Seed данные - 6 брендов (Zara, H&M, Nike, Adidas, Uniqlo, Gucci)

### Шаг 2: Добавили связь products → brands

**Команда:**
```bash
supabase migration new add_brand_to_products
```

**Что создалось:**
- Файл: `supabase/migrations/20260210085846_add_brand_to_products.sql`

**Что внутри миграции:**
1. ✅ Добавили колонку `brand_id` в таблицу `products`
2. ✅ Создали foreign key связь с таблицей `brands`
3. ✅ Создали индекс `idx_products_brand_id`
4. ✅ Обновили существующие товары, привязав их к брендам
5. ✅ Создали VIEW `products_with_brands` для удобных запросов
6. ✅ Создали функцию `get_products_by_brand(brand_name)`

### Шаг 3: Применили миграции локально

**Команда:**
```bash
supabase db reset
```

**Что произошло:**
1. Удалена локальная БД
2. Создана новая БД
3. Применены ВСЕ миграции по порядку:
   - `20260127095621` - Initial schema (profiles, products, cart_items)
   - `20260128125046` - Email logs
   - `20260128142000` - Resend ID
   - `20260210085414` - **Brands table** ← наша новая
   - `20260210085846` - **Brand to products** ← наша новая
4. Применен seed.sql (дополнительные тестовые данные)

**Результат:**
```
✅ Successfully created brands table with 6 brands
✅ Successfully linked existing products to brands
📊 Products with brand: 3
📊 Products without brand: 1
```

---

## 🔧 Команды которые использовались

### 1. Проверка статуса Supabase
```bash
supabase status
# Показывает: запущен ли локальный Supabase, URL, ключи
```

### 2. Создание новой миграции
```bash
supabase migration new <название>
# Пример: supabase migration new create_brands_table
# Создает файл: supabase/migrations/TIMESTAMP_название.sql
```

### 3. Применение миграций (полный сброс)
```bash
supabase db reset
# ⚠️ УДАЛЯЕТ ВСЕ ДАННЫЕ!
# Пересоздает БД с нуля
# Применяет все миграции по порядку
# Применяет seed.sql
```

### 4. Проверка различий схемы
```bash
supabase db diff --use-migra
# Показывает различия между:
# - Текущей БД
# - Схемой после применения всех миграций
# Если вывод пустой = все ОК
```

### 5. Список миграций
```bash
supabase migration list
# Показывает:
# - Локальные миграции
# - Удаленные миграции (на Dev/Prod)
# - Какие миграции еще не применены на удаленном сервере
```

### 6. Применение миграций на удаленный сервер
```bash
# Сначала подключаемся к нужному проекту
supabase link --project-ref fugpgfraaxeugjcctzkk  # Dev
# или
supabase link --project-ref vdpvmxzcdjtjkiwylpuv  # Production

# Затем применяем миграции
supabase db push
# ⚠️ Применяет ТОЛЬКО новые миграции (которых нет на удаленном сервере)
```

### 7. Проверка Git статуса
```bash
git status
# Показывает новые файлы миграций
```

---

## 🌱 Работа с seed данными

### Что такое seed данные?

**Seed данные** - это тестовые данные для разработки и тестирования.

### Два способа добавления seed данных:

#### Способ 1: Внутри миграции (как мы сделали)

**Плюсы:**
- ✅ Данные применяются автоматически вместе с миграцией
- ✅ Версионируются вместе со схемой
- ✅ Одинаковые данные на всех окружениях

**Минусы:**
- ❌ Данные нельзя легко удалить (они часть миграции)
- ❌ При каждом `db reset` данные пересоздаются

**Пример (внутри миграции):**
```sql
-- В файле миграции 20260210085414_create_brands_table.sql
INSERT INTO public.brands (name, description, country, founded_year) VALUES
  ('Zara', 'Испанский бренд быстрой моды', 'Spain', 1975),
  ('Nike', 'Американский спортивный бренд', 'USA', 1964);
```

#### Способ 2: Отдельный файл seed.sql

**Плюсы:**
- ✅ Можно легко изменять без создания миграций
- ✅ Применяется только локально (не на Production)
- ✅ Удобно для больших объемов тестовых данных

**Минусы:**
- ❌ Нужно помнить применять вручную
- ❌ Может отличаться на разных окружениях

**Файл:** `supabase/seed.sql`

```sql
-- Этот файл применяется автоматически после миграций при `supabase db reset`

-- Создаем тестовых пользователей
-- ⚠️ ВАЖНО: Сначала нужно создать пользователей через Supabase Auth!

-- Пример: добавление ролей для тестовых пользователей
INSERT INTO public.user_roles (user_id, role) VALUES
  ('00000000-0000-0000-0000-000000000001', 'admin'),
  ('00000000-0000-0000-0000-000000000002', 'customer')
ON CONFLICT (user_id, role) DO NOTHING;

-- Добавляем профили для тестовых пользователей
INSERT INTO public.profiles (user_id, name, email) VALUES
  ('00000000-0000-0000-0000-000000000001', 'Admin User', 'admin@test.com'),
  ('00000000-0000-0000-0000-000000000002', 'Test Customer', 'customer@test.com')
ON CONFLICT (user_id) DO NOTHING;

-- Добавляем тестовые товары в корзину
INSERT INTO public.cart_items (user_id, product_id, quantity, size, color)
SELECT
  '00000000-0000-0000-0000-000000000002',
  id,
  1,
  sizes[1],
  colors[1]
FROM public.products
LIMIT 2
ON CONFLICT (user_id, product_id, size, color) DO NOTHING;
```

---

## 🏆 Best Practices для Production

### 1. Всегда тестируйте миграции локально

```bash
# ❌ НИКОГДА не делайте так:
supabase db push  # Сразу на Production без тестирования

# ✅ ПРАВИЛЬНО:
supabase db reset              # Тестируем локально
supabase db diff --use-migra   # Проверяем что нет различий
# Только потом деплоим на Production
```

### 2. Используйте транзакции

```sql
-- ✅ ПРАВИЛЬНО: Вся миграция в одной транзакции
BEGIN;

ALTER TABLE products ADD COLUMN brand_id UUID;
CREATE INDEX idx_products_brand_id ON products(brand_id);

COMMIT;

-- Если что-то пойдет не так - все откатится автоматически
```

### 3. Делайте миграции обратимыми (reversible)

```sql
-- ❌ ПЛОХО: Нельзя откатить
ALTER TABLE products DROP COLUMN old_field;

-- ✅ ХОРОШО: Можно откатить
ALTER TABLE products ADD COLUMN new_field TEXT;
-- Если нужно откатить - создаем новую миграцию:
-- ALTER TABLE products DROP COLUMN new_field;
```

### 4. Не изменяйте старые миграции

```bash
# ❌ НИКОГДА не редактируйте файлы миграций, которые уже применены на Production!
# Это сломает историю миграций

# ✅ ПРАВИЛЬНО: Создавайте новую миграцию для исправления
supabase migration new fix_brands_table
```

### 5. Используйте осмысленные имена

```bash
# ❌ ПЛОХО
supabase migration new update_db
supabase migration new fix

# ✅ ХОРОШО
supabase migration new add_brand_id_to_products
supabase migration new create_email_logs_table
supabase migration new add_index_to_products_category
```

### 6. Добавляйте комментарии

```sql
-- ✅ ХОРОШО: Понятно что и зачем
COMMENT ON TABLE brands IS 'Таблица для хранения информации о брендах одежды';
COMMENT ON COLUMN brands.founded_year IS 'Год основания бренда (1800-2026)';
```

### 7. Проверяйте производительность

```sql
-- Для больших таблиц создавайте индексы CONCURRENTLY
CREATE INDEX CONCURRENTLY idx_products_brand_id ON products(brand_id);
-- Это не блокирует таблицу во время создания индекса
```

---

## 🚀 Деплой через Pull Request (Production способ)

Это **самый безопасный и профессиональный** способ для Production.

### Workflow:

```
┌─────────────────────────────────────────────────────────────┐
│  Local → Feature Branch → PR → Review → Merge → Deploy     │
└─────────────────────────────────────────────────────────────┘
```

### Шаг 1: Создайте feature branch

```bash
# Создайте новую ветку для миграций
git checkout -b feature/add-brands-table

# Создайте миграции
supabase migration new create_brands_table
# ... редактируйте миграцию ...

# Протестируйте локально
supabase db reset
supabase db diff --use-migra

# Закоммитьте
git add supabase/migrations/
git commit -m "feat: add brands table and link to products"
git push origin feature/add-brands-table
```

### Шаг 2: Создайте Pull Request

```bash
# На GitHub:
# 1. Откройте репозиторий
# 2. Нажмите "New Pull Request"
# 3. Base: main (или develop)
# 4. Compare: feature/add-brands-table
# 5. Заполните описание:
```

**Пример описания PR:**

```markdown
## 🎯 Цель

Добавить таблицу брендов и связать её с товарами

## 📝 Изменения

### Миграции:
- `20260210085414_create_brands_table.sql`
  - Создана таблица `brands`
  - Добавлены RLS политики
  - Добавлены seed данные (6 брендов)

- `20260210085846_add_brand_to_products.sql`
  - Добавлено поле `brand_id` в `products`
  - Создан VIEW `products_with_brands`
  - Создана функция `get_products_by_brand()`

## ✅ Тестирование

- [x] Миграции применены локально без ошибок
- [x] `supabase db diff` показывает "No schema changes"
- [x] Seed данные корректно загружаются
- [x] RLS политики работают корректно

## 🚀 План деплоя

1. Merge PR в `main`
2. Применить миграции на Dev: `supabase db push --project-ref fugpgfraaxeugjcctzkk`
3. Проверить на Dev
4. Применить на Production: `supabase db push --project-ref vdpvmxzcdjtjkiwylpuv`

## ⚠️ Риски

- Низкий риск: только добавление новых таблиц и полей
- Существующие данные не затрагиваются
- Можно откатить через новую миграцию

## 📸 Screenshots

[Приложите скриншоты из Supabase Studio]
```

### Шаг 3: Code Review

**Что проверять в PR с миграциями:**

✅ **Checklist для ревьюера:**
- [ ] Миграции имеют осмысленные имена
- [ ] Используются транзакции (BEGIN/COMMIT)
- [ ] Добавлены индексы для foreign keys
- [ ] RLS политики настроены корректно
- [ ] Нет DROP TABLE/DROP COLUMN без бэкапа
- [ ] Комментарии к таблицам и полям
- [ ] Seed данные не содержат реальных пользовательских данных
- [ ] Миграции протестированы локально

### Шаг 4: Merge и Deploy

```bash
# После одобрения PR:

# 1. Merge PR на GitHub
# (через UI или командой)
git checkout main
git pull origin main

# 2. Деплой на Dev (Staging)
supabase link --project-ref fugpgfraaxeugjcctzkk
supabase db push

# 3. Проверка на Dev
# Откройте Dev приложение и проверьте что все работает
# https://fugpgfraaxeugjcctzkk.supabase.co

# 4. Если все ОК - деплой на Production
supabase link --project-ref vdpvmxzcdjtjkiwylpuv
supabase db push

# 5. Проверка на Production
# Откройте Production приложение
```

### Шаг 5: Автоматизация через GitHub Actions (опционально)

Создайте `.github/workflows/deploy-migrations.yml`:

```yaml
name: Deploy Migrations

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/migrations/**'

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy to Dev
        run: |
          supabase link --project-ref ${{ secrets.SUPABASE_DEV_PROJECT_REF }}
          supabase db push
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Notify Slack
        run: |
          curl -X POST ${{ secrets.SLACK_WEBHOOK }} \
            -d '{"text":"✅ Migrations deployed to Dev"}'

  deploy-production:
    runs-on: ubuntu-latest
    needs: deploy-dev
    environment: production  # Требует ручного подтверждения
    steps:
      - uses: actions/checkout@v3

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1

      - name: Deploy to Production
        run: |
          supabase link --project-ref ${{ secrets.SUPABASE_PROD_PROJECT_REF }}
          supabase db push
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
```

---

## 🔄 Альтернативные способы деплоя

### Способ 1: Прямой push (для Dev/Staging)

**Когда использовать:** Быстрые изменения на Dev окружении

```bash
# Создали миграцию
supabase migration new add_new_field

# Протестировали локально
supabase db reset

# Сразу деплоим на Dev
supabase link --project-ref fugpgfraaxeugjcctzkk
supabase db push

# Коммитим в Git
git add supabase/migrations/
git commit -m "feat: add new field"
git push
```

**Плюсы:** ⚡ Быстро
**Минусы:** ⚠️ Нет code review, можно сломать Dev

### Способ 2: Через Supabase Dashboard (GUI)

**Когда использовать:** Экстренные hotfix на Production

```bash
# 1. Откройте Supabase Dashboard
open https://supabase.com/dashboard/project/vdpvmxzcdjtjkiwylpuv

# 2. SQL Editor → New query
# 3. Напишите SQL:
ALTER TABLE products ADD COLUMN urgent_field TEXT;

# 4. Run
# 5. Создайте миграцию локально для синхронизации:
supabase db pull
```

**Плюсы:** 🚨 Очень быстро для hotfix
**Минусы:** ⚠️ Легко забыть создать миграцию, рассинхронизация

### Способ 3: Через CI/CD с автотестами

**Когда использовать:** Enterprise Production

```yaml
# .github/workflows/test-migrations.yml
name: Test Migrations

on:
  pull_request:
    paths:
      - 'supabase/migrations/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Start Supabase
        run: |
          supabase start
          supabase db reset

      - name: Run tests
        run: |
          npm run test:migrations

      - name: Check schema
        run: |
          supabase db diff --use-migra
```

**Плюсы:** ✅ Максимальная безопасность
**Минусы:** ⏱️ Требует настройки

---

## ⏪ Откат миграций

### Важно понять:

```
⚠️ Supabase НЕ поддерживает автоматический откат миграций!
Нужно создавать новую миграцию для отката.
```

### Способ 1: Создать обратную миграцию (рекомендуется)

```bash
# Допустим, нужно откатить добавление brand_id

# 1. Создаем новую миграцию
supabase migration new rollback_brand_id_from_products

# 2. Пишем обратные операции:
```

```sql
-- supabase/migrations/20260210090000_rollback_brand_id_from_products.sql

BEGIN;

-- Удаляем VIEW
DROP VIEW IF EXISTS public.products_with_brands;

-- Удаляем функцию
DROP FUNCTION IF EXISTS public.get_products_by_brand;

-- Удаляем индекс
DROP INDEX IF EXISTS public.idx_products_brand_id;

-- Удаляем колонку
ALTER TABLE public.products DROP COLUMN IF EXISTS brand_id;

COMMIT;
```

```bash
# 3. Применяем
supabase db reset  # Локально
supabase db push   # На Production
```

### Способ 2: Восстановление из бэкапа (для критических ситуаций)

```bash
# 1. Откройте Supabase Dashboard
open https://supabase.com/dashboard/project/vdpvmxzcdjtjkiwylpuv/settings/backups

# 2. Выберите бэкап до применения миграции
# 3. Нажмите "Restore"

# ⚠️ ВНИМАНИЕ: Это восстановит ВСЮ базу данных!
# Все изменения после бэкапа будут потеряны!
```

### Способ 3: Ручной откат через SQL

```sql
-- Если миграция только что применена и нужно быстро откатить:

-- 1. Найдите миграцию в таблице
SELECT * FROM supabase_migrations.schema_migrations
ORDER BY version DESC LIMIT 5;

-- 2. Удалите запись о миграции (НЕ рекомендуется!)
DELETE FROM supabase_migrations.schema_migrations
WHERE version = '20260210085846';

-- 3. Вручную откатите изменения
ALTER TABLE products DROP COLUMN brand_id;

-- ⚠️ Это опасно! Используйте только в крайнем случае!
```

---

## 🔍 Troubleshooting

### Проблема 1: Миграция не применяется

```bash
# Ошибка: "migration already applied"

# Решение 1: Проверьте список миграций
supabase migration list

# Решение 2: Если миграция есть в Remote, но не применена:
supabase migration repair --status applied 20260210085414

# Решение 3: Если миграция сломана:
supabase migration repair --status reverted 20260210085414
```

### Проблема 2: Различия в схеме после reset

```bash
# Ошибка: "supabase db diff" показывает изменения

# Причина: Вы изменили схему через Studio, но не создали миграцию

# Решение: Создайте миграцию из различий
supabase db diff -f fix_schema_differences

# Проверьте созданный файл и примените
supabase db reset
```

### Проблема 3: Конфликт миграций в разных ветках

```bash
# Ситуация:
# - В ветке A создана миграция 20260210_090000_feature_a.sql
# - В ветке B создана миграция 20260210_090000_feature_b.sql
# - Одинаковый timestamp!

# Решение: Переименуйте одну из миграций
mv supabase/migrations/20260210_090000_feature_b.sql \
   supabase/migrations/20260210_090001_feature_b.sql
```

### Проблема 4: Seed данные не применяются

```bash
# Проверьте что seed.sql существует
ls -la supabase/seed.sql

# Проверьте синтаксис
supabase db reset --debug

# Если seed.sql не применяется автоматически:
supabase db reset
# Затем вручную:
psql postgresql://postgres:postgres@127.0.0.1:54322/postgres -f supabase/seed.sql
```

---

## 📚 Полезные ссылки

- [Supabase CLI Reference](https://supabase.com/docs/reference/cli)
- [Database Migrations Guide](https://supabase.com/docs/guides/cli/local-development#database-migrations)
- [Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)

---

## 🎓 Резюме

### Что мы изучили:

1. ✅ Как создавать миграции (`supabase migration new`)
2. ✅ Как применять миграции локально (`supabase db reset`)
3. ✅ Как добавлять seed данные (в миграции или seed.sql)
4. ✅ Как деплоить на Production через PR (безопасный способ)
5. ✅ Как откатывать миграции (обратная миграция)
6. ✅ Best practices для Production

### Основные команды:

```bash
# Разработка
supabase migration new <name>    # Создать миграцию
supabase db reset                # Применить все миграции локально
supabase db diff --use-migra     # Проверить различия

# Деплой
supabase link --project-ref XXX  # Подключиться к проекту
supabase db push                 # Применить миграции на удаленный сервер
supabase migration list          # Список миграций

# Проверка
supabase status                  # Статус локального Supabase
open http://127.0.0.1:54323      # Открыть Studio
```

### Золотое правило:

```
🏆 Всегда тестируйте миграции локально перед деплоем на Production!
🏆 Используйте Pull Request для code review миграций!
🏆 Никогда не редактируйте старые миграции!
```

---

**Автор:** Migration Training Session
**Дата:** 2026-02-10
**Версия:** 1.0

