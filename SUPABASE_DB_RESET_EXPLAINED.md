# 🔄 Команда `supabase db reset` - Подробное объяснение

## 🎯 Что делает команда?

```bash
supabase db reset
```

### Пошаговое выполнение:

```
┌─────────────────────────────────────────────────────────────┐
│  ШАГ 1: Удаление текущей базы данных                        │
│  ⚠️  DROP DATABASE postgres;                                │
│  ⚠️  Все данные удаляются!                                  │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  ШАГ 2: Создание новой пустой базы данных                   │
│  ✅  CREATE DATABASE postgres;                              │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  ШАГ 3: Инициализация базовой схемы Supabase                │
│  ✅  Создание схем: auth, storage, realtime, public         │
│  ✅  Создание служебных таблиц                              │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  ШАГ 4: Применение ВСЕХ миграций по порядку                 │
│  ✅  20260127095621_initial.sql                             │
│  ✅  20260128125046_email_logs.sql                          │
│  ✅  20260128142000_resend_id.sql                           │
│  ✅  20260210085414_create_brands_table.sql                 │
│  ✅  20260210085846_add_brand_to_products.sql               │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  ШАГ 5: Применение seed.sql (если существует)               │
│  ✅  Вставка тестовых данных                                │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│  ШАГ 6: Перезапуск контейнеров                              │
│  ✅  Restart PostgREST, GoTrue, Storage и др.               │
└─────────────────────────────────────────────────────────────┘
```

---

## ⚠️ БЕЗОПАСНОСТЬ: Когда использовать?

### ✅ БЕЗОПАСНО использовать:

#### 1. **Локальная разработка (Docker)**
```bash
# Это ваш локальный Supabase в Docker
supabase db reset

# ✅ Абсолютно безопасно!
# - Удаляет только локальную БД
# - Не затрагивает Dev/Production
# - Можно делать сколько угодно раз
```

**Почему безопасно:**
- 🔒 Работает только с локальным Docker контейнером
- 🔒 Не имеет доступа к удаленным серверам
- 🔒 Все данные тестовые (из seed.sql)
- 🔒 Можно пересоздать за секунды

#### 2. **Тестирование миграций**
```bash
# Создали новую миграцию
supabase migration new add_new_field

# Хотим протестировать
supabase db reset  # ✅ Безопасно

# Нашли ошибку, исправили
# Редактируем миграцию
supabase db reset  # ✅ Снова безопасно

# Повторяем пока не заработает
```

#### 3. **Синхронизация с миграциями**
```bash
# Переключились на другую ветку Git
git checkout feature/new-feature

# Там другие миграции
supabase db reset  # ✅ Применит миграции из новой ветки

# Вернулись обратно
git checkout main
supabase db reset  # ✅ Вернет состояние БД
```

#### 4. **Очистка "мусора" после экспериментов**
```bash
# Вы экспериментировали в Studio:
# - Создали таблицы вручную
# - Добавили тестовые данные
# - Изменили схему

# Хотите вернуться к чистому состоянию
supabase db reset  # ✅ Вернет к миграциям
```

---

### ❌ ОПАСНО использовать:

#### 1. **На Production сервере**
```bash
# ❌❌❌ НИКОГДА НЕ ДЕЛАЙТЕ ТАК! ❌❌❌
supabase link --project-ref vdpvmxzcdjtjkiwylpuv  # Production
supabase db reset  # ⚠️⚠️⚠️ УДАЛИТ ВСЕ ДАННЫЕ ПОЛЬЗОВАТЕЛЕЙ!

# Результат:
# 💥 Все пользователи удалены
# 💥 Все заказы удалены
# 💥 Вся история удалена
# 💥 Компания потеряла деньги
# 💥 Вас уволят
```

**ВАЖНО:** `supabase db reset` работает ТОЛЬКО локально!
Команда не работает на удаленных проектах (Dev/Production).

#### 2. **Когда есть важные локальные данные**
```bash
# Вы создали тестовых пользователей вручную
# Настроили сложные данные для тестирования
# Потратили час на подготовку

supabase db reset  # ❌ Все удалится!

# Решение: Сохраните данные в seed.sql перед reset
```

---

## 🔍 Проверка: Что будет удалено?

### Перед выполнением `supabase db reset`:

```bash
# 1. Проверьте что вы на локальном окружении
supabase status
# Должно быть: http://127.0.0.1:54321

# 2. Проверьте к какому проекту подключены
cat .git/config | grep supabase
# Или
supabase link --project-ref  # Без параметра покажет текущий

# 3. Сделайте дамп важных данных (если нужно)
pg_dump postgresql://postgres:postgres@127.0.0.1:54322/postgres > backup.sql
```

---

## 🆚 Альтернативы `supabase db reset`

### 1. **Применить только новые миграции** (без удаления данных)

```bash
# ❌ Удаляет все данные
supabase db reset

# ✅ Применяет только новые миграции, данные сохраняются
supabase db push
```

**Когда использовать:**
- Есть важные тестовые данные
- Хотите добавить новую миграцию без потери данных
- Тестируете инкрементальные изменения

**Пример:**
```bash
# Есть данные в БД
# Создали новую миграцию
supabase migration new add_column

# Применяем только новую миграцию
supabase db push  # ✅ Данные сохранятся
```

### 2. **Откатить конкретную миграцию**

```bash
# Создайте обратную миграцию
supabase migration new rollback_last_change

# В файле напишите обратные операции
# ALTER TABLE products DROP COLUMN brand_id;

# Примените
supabase db push
```

### 3. **Восстановить из дампа**

```bash
# Сделайте дамп перед экспериментами
pg_dump postgresql://postgres:postgres@127.0.0.1:54322/postgres > backup.sql

# Экспериментируйте...

# Восстановите если что-то пошло не так
supabase db reset
psql postgresql://postgres:postgres@127.0.0.1:54322/postgres < backup.sql
```

### 4. **Пересоздать только контейнеры** (без изменения БД)

```bash
# Перезапустить сервисы без изменения БД
supabase stop
supabase start

# Или только PostgREST
docker restart supabase_rest_vdpvmxzcdjtjkiwylpuv
```

---

## 📊 Сравнение команд

| Команда | Удаляет данные? | Применяет миграции? | Безопасно? | Когда использовать |
|---------|----------------|---------------------|------------|-------------------|
| `supabase db reset` | ✅ Да, все | ✅ Да, все | ✅ Локально | Тестирование миграций |
| `supabase db push` | ❌ Нет | ✅ Только новые | ✅ Везде | Деплой новых миграций |
| `supabase stop/start` | ❌ Нет | ❌ Нет | ✅ Везде | Перезапуск сервисов |
| `DROP DATABASE` | ✅ Да, все | ❌ Нет | ❌ Опасно | Никогда вручную |

---

## 🎓 Best Practices

### ✅ Правильное использование:

```bash
# 1. Создали миграцию
supabase migration new my_changes

# 2. Редактируем SQL файл
vim supabase/migrations/TIMESTAMP_my_changes.sql

# 3. Тестируем локально
supabase db reset  # ✅ Безопасно

# 4. Проверяем результат
open http://127.0.0.1:54323

# 5. Если ошибка - исправляем и повторяем
# Редактируем миграцию
supabase db reset  # ✅ Снова безопасно

# 6. Когда все работает - коммитим
git add supabase/migrations/
git commit -m "feat: my changes"

# 7. Деплоим на Dev БЕЗ reset
supabase link --project-ref fugpgfraaxeugjcctzkk
supabase db push  # ✅ Только новые миграции, данные сохраняются
```

### ❌ Неправильное использование:

```bash
# ❌ ПЛОХО: Reset на Production
supabase link --project-ref vdpvmxzcdjtjkiwylpuv
supabase db reset  # Не работает, но если бы работало - катастрофа!

# ❌ ПЛОХО: Reset без сохранения важных данных
# Потратили час на создание тестовых данных
supabase db reset  # Все потеряно!

# ❌ ПЛОХО: Reset вместо push
# Хотим применить одну миграцию на Dev
supabase db reset  # Удалит все данные на Dev!
# Правильно: supabase db push
```

---

## 🔐 Защита от случайного удаления

### Способ 1: Алиас с подтверждением

Добавьте в `~/.bashrc` или `~/.zshrc`:

```bash
# Безопасный reset с подтверждением
alias supabase-reset='echo "⚠️  This will DELETE ALL local data! Continue? (y/n)" && read answer && [ "$answer" = "y" ] && supabase db reset || echo "Cancelled"'

# Использование:
supabase-reset
# ⚠️  This will DELETE ALL local data! Continue? (y/n)
# y  ← нужно подтвердить
```

### Способ 2: Автоматический бэкап перед reset

```bash
# Создайте скрипт safe-reset.sh
#!/bin/bash

echo "📦 Creating backup before reset..."
BACKUP_FILE="backup_$(date +%Y%m%d_%H%M%S).sql"
pg_dump postgresql://postgres:postgres@127.0.0.1:54322/postgres > "$BACKUP_FILE"
echo "✅ Backup saved to: $BACKUP_FILE"

echo "🔄 Running supabase db reset..."
supabase db reset

echo "✅ Done! Backup available at: $BACKUP_FILE"
```

```bash
# Использование:
chmod +x safe-reset.sh
./safe-reset.sh
```

---

## 📝 Резюме

### Когда использовать `supabase db reset`:

✅ **ДА:**
- Локальная разработка (Docker)
- Тестирование новых миграций
- Исправление ошибок в миграциях
- Переключение между ветками Git
- Очистка после экспериментов
- Синхронизация с актуальными миграциями

❌ **НЕТ:**
- Production сервер (команда не работает)
- Dev сервер с важными данными
- Когда нужно применить только новые миграции
- Когда есть несохраненные тестовые данные

### Главное правило:

```
🏆 supabase db reset = "Пересоздать БД с нуля из миграций"

✅ Безопасно ТОЛЬКО локально (Docker)
❌ Удаляет ВСЕ данные
✅ Применяет ВСЕ миграции заново
✅ Идеально для тестирования
```

### Золотое правило безопасности:

```bash
# Перед ЛЮБОЙ опасной командой:
supabase status  # Проверьте что это локальный сервер!

# Должно быть:
# Project URL: http://127.0.0.1:54321  ← Локально ✅

# НЕ должно быть:
# Project URL: https://xxx.supabase.co  ← Удаленно ❌
```

---

**Вывод:** `supabase db reset` - это мощный инструмент для локальной разработки, но требует понимания что он делает. Используйте его смело локально, но НИКОГДА на Production!

